# ============================================================
#  CLIENT — LOCAL development  (Vite mode: development)
#  freelancer-frontend-react/.env.development
#
#  Auto-loaded by `npm run dev`. COMMITTED ON PURPOSE.
#
#  VITE_ values are embedded into the browser bundle, so they are
#  public by design. NEVER put secrets (JWT, DB, passwords) here.
# ============================================================

# Local Spring Boot backend
VITE_API_URL=http://localhost:8080
