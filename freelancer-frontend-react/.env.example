# TEMPLATE only. The real files (committed, public VITE_* values):
#   .env.development  -> local   (npm run dev)
#   .env.production   -> Vercel  (npm run build / dashboard)
# See ../ENV_SETUP.md
#
# Local dev (Spring Boot default)
VITE_API_URL=http://localhost:8080

# Production example (your Railway backend public URL):
# VITE_API_URL=https://<service>.up.railway.app
